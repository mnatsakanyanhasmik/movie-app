export class MovieDuration {
    public hours: number;
    public minutes: number;

    constructor(
        hours?: number, 
        minutes?: number) {
        this.hours = hours;
        this.minutes = minutes;
    }
}